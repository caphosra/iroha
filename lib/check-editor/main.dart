import "package:flutter/material.dart";

class IrohaCheckEditor extends StatelessWidget {
    IrohaCheckEditor({Key? key}) : super(key: key);

    @override
    Widget build(BuildContext context) {
        return Container(
			
		);
    }
}
