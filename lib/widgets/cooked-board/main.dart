import "package:flutter/material.dart";

class IrohaCookedBoard extends StatelessWidget {
    IrohaCookedBoard({Key? key}) : super(key: key);

    @override
    Widget build(BuildContext context) {
		return Container();
	}
}
